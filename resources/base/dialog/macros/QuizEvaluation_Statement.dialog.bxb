template-macro-def (quiz-evaluation-statement) {
  params {
    param (quizState) {
      type (QuizState)
      min (Required)
      max (One)
    }
  }
  content {
    template("That's #{number(quizState.correctAnswerCount)} out of #{number(quizState.currentQuestionNumber)}!")
  }
}