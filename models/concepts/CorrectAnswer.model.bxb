structure (CorrectAnswer) {
  description (Holds information on the correct answer)

  property (acceptedAnswers) {
    description (All accepted answers)
    type (core.Text)
    min (Required) max (Many)
    visibility (Private)
  }

  property (explanation) {
    type (core.Text)
    min (Optional) max (One)
    visibility (Private)
  }
}
